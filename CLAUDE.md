# Life Re-Design System

Notion連携の個人用ライフデザインシステム。GitHub `Kame-223/Home-pages-for-Notion` → Vercel自動デプロイ。
単一ファイル `public/index.html` + プロキシ（notion.js / ai.js / fetch-title.js）。

## 設計書の位置づけ

- `PHILOSOPHY.md` と `docs/chapter*.md` は「絶対に守るべき仕様書」ではない。方向性の参考であり、実装を進める中で内容が変わることは前提。
- 実装中に設計書と違う判断をしても失敗ではない。むしろ設計書側を書き換えるべきサインとして扱う。
- 逆に、コードだけ変えて設計書を放置し続けると、両者が乖離して参照する意味を失う。大きく変えたときは設計書側も一言更新する。

## 参照ファイル（毎回は読まない。必要なときだけ）

- `PHILOSOPHY.md` — なぜこう作るか。UX判断で迷ったときだけ読む。
- `docs/00-index.md` — 設計書の目次。
- `docs/chapter1-philosophy.md` 〜 `docs/chapter6-emode.md` — 各章の詳細。**今作業している章のファイルだけ読む。他の章は開かない。**
- `docs/implementation-notes.md` — 実装済み機能・カラー設定・Notion DB IDなど、現在のコード実装状況のメモ。

## 作業の進め方

- 単一セッションで進める。並行ターミナルは使わない。
- 次のいずれかに当てはまる作業は、必ず Plan Mode を通す：3ファイル以上にまたがる／新しいNotionプロパティやDB構造を追加する／既存の関数の挙動を変える。それ以外（誤字修正・1行の値変更・スタイルの微調整）は直接でよい。
- Plan Modeの計画が5ステップを超える場合は、1ステップごとに止めて見せる。5ステップ以下は最後まで通してよい。
- 実行を承認する前に、それが何をするコマンドか日本語で一行説明する。
- 訂正が2回続いたら、3回目に入る前にセッションを仕切り直す（`/clear`）。
- 各ステップの完了ごとに Git コミットする。
- 通常の `git push` は確認不要で自動実行してよい。ファイル削除・上書きなど、より大胆な破壊的操作は事前確認が必須。

## 判断の担当

- コード内部の実装方法（変数名、関数分割、ライブラリ選定）は Claude が決めてよい。
- 画面に表示される見た目・文言・操作の手触りに関わる変更は、必ずコウセイに選択肢を2〜3個、日本語で提示してから進める。ここは Claude が一次判定しない。迷ったら `PHILOSOPHY.md` を読んでから提示する。

## 動作確認

- 画面の見た目・操作に関わる変更を完了報告する前には、必ず `run` スキル（Playwright）で実際に動かして確認する。誤字修正・1行の値変更のような軽微な変更は対象外。

## テーマ

- ダークテーマは現在機能停止中（`initTheme()` を light に固定、トグルボタン非表示）。今後の編集・見た目調整はすべてライトテーマ（`html[data-theme="light"]`）を対象に行う。ダークテーマ用CSSはコード上に残すが、動作確認・調整の対象外。

## 禁止事項

- `.env` ファイル、シークレット、認証情報の読み書き
- `git push --force`
- 確認なしでの破壊的操作（削除・上書き）

## `/goal` の使用条件

- 設計・思想を固める作業では使わない。
- 実装フェーズで、`docs/chapter*.md` に受け入れ基準が明文化された機能にのみ使う。

## サブエージェント

- 同時実行数の上限は2個まで。
- `public/index.html` への書き込みは常に同時1個まで（全ページがこの1ファイルに同居しているため、ページ単位で分けても同時書き込みの安全性は上がらない）。2個目のスロットは完全に別ファイル（`api/*.js`、`docs/*.md`、Notion側の作業など）にのみ使う。
- バックグラウンドで実行する作業（判断がすでに確定している機械的な変更）は Sonnet を使う。
- コストが並列数に対してほぼ掛け算で増えるため、詳細は `ADVANCED.md` を参照。

## 議論用モデルの使い分け

- この会話（フォアグラウンド）のモデルはコウセイが `/model` で切り替える。Claude側から自動で切り替えることはできないため、該当する話題になったらClaudeが「切り替えた方がいいかも」と声をかける。
- デフォルトは Sonnet。
- 以下に該当する話題のときだけ Opus への切り替えを提案する：
  1. 要件があいまいで認識がズレるリスクが高い設計判断（状態遷移が複雑なUI設計など）
  2. 見た目・文言・手触りの微妙なニュアンス判断
  3. 複数ファイルやNotionのデータ構造にまたがる設計判断
  4. すでに2回訂正が入った話題（`/clear`する前に、その話題だけOpusに切り替えてから再開する）
- 選択肢がすでに絞られている確認や機械的な整理・報告は Sonnet のままでよい。

---

## Notion API作業時の注意

⚠️ Notionのプロパティ（特にselect/statusのoptions）をPATCHする前は、`notion-api-safety` skillを必ず確認する。過去にoption配列の全上書きで既存ページのデータが消失した事故がある。
