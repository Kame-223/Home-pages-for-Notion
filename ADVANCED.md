# 高度な機能の備忘録

**今は使わないが、忘れないための記録。**

現段階（憲法策定〜実装初期）では、これらは不要と判断した。
理由：どれも「セットアップ自体が新しい学習負荷になる」ため。
憲法 + Plan Mode + 単一セッションで十分であり、慣れてから足せばよい。

以下は、**将来こういう選択肢がある**という記録である。

---

## 1. Hooks（フック）

### 何か
Claude Code のライフサイクル上の特定のタイミング（ファイル編集の前後、セッション開始時など）で、
**決定論的に**スクリプトを実行する仕組み。

### なぜ重要か
CLAUDE.md の指示は **約70〜80%しか守られない**。
「絶対にやってはいけない」ルールに対して、この遵守率は不十分。
Hooks は 100% 実行されるため、CLAUDE.md では防げないことを防げる。

### 使いどころ（将来）
- ファイル編集のたびに自動でコード整形する
- 危険なシェルコマンドを実行前にブロックする
- 編集後に自動でテストを走らせる

### 注意
- 「書き込みを途中でブロックする」のは避けるべき。複数ステップの推論が壊れる。
  書き終わってから検証する形にする。

### 今すぐ入れる簡易版
フル Hooks の代わりに、`.claude/settings.json` の **permissions 設定**を使う。
これは「これは許可／これは禁止」を書くだけで、Hooks より簡単。
deny に指定したファイルは Claude から「見えなく」なるため、ブロックより安全。
→ これは憲法（第5条）に採用済み。

---

## 2. Subagents（サブエージェント）

### 何か
親の Claude が、**独自のコンテキストウィンドウを持つ別の Claude** に作業を委任する仕組み。
子の作業ログ（ファイル検索、大量の出力、試行錯誤）は親に流れ込まず、要約だけが返る。

### なぜ重要か
「文脈の汚染」を構造的に防げる。
失敗した試行錯誤が親のコンテキストに溜まらないので、親は綺麗な状態を保てる。

### 使いどころ（将来）
- コードベースの調査（探索の騒がしさを親に持ち込まない）
- 実装後の検証・レビューを、別の Claude にやらせる（自分の作業を自分で採点させない）
- 「管理者／作業者」の階層構造を作る場合の基盤

### 今保留にした理由
- 思想がまだ実装の中で安定していない
- 翻訳の劣化リスク（「メンタル保護的にNG」というニュアンスが技術指示に落ちる過程で削げる）
- API 呼び出しが増えてコストがかかる
- 問題が起きたとき、管理者と作業者のどちらが悪いか切り分けが面倒

### コストについて（要注意）

サブエージェントは**独立したコンテキストウィンドウ**を持つため、親セッションを引き継がず、必要な情報を毎回ゼロから読み直す。
結果として、**3つのサブエージェントを使うタスクは、単一セッションの約4倍のトークンを消費する**。
ある報告では、重いセッションのトークン使用量の**85%がサブエージェント絡み**だったという。

**危険な実例**：「このコードベースを深くレビューして。複数のサブエージェントを使っていい」という**曖昧な指示**だけで、
- 49個のサブエージェントが並列で2.5時間走り、推定8,000〜15,000ドル
- 23個のサブエージェントが無人で3日間走り、47,000ドル
- ある利用者は、同様の曖昧な指示だけで**週次利用枠の50%を30秒で消費**

**将来もし使うなら**：
- 個数の上限を必ず明記する（「一度に3個まで」など）
- 「複数のサブエージェントを使っていい」のような曖昧な許可は絶対に出さない
- Pro/Maxのようなサブスクリプションでは直接の請求ではなく、5時間・週ごとの利用枠を一瞬で失う形で被害が出る

---

## 3. Skills（スキル）

### 何か
`.claude/skills/<名前>/SKILL.md` というフォルダに置く、**再利用可能な作業手順書**。
`/名前` で呼び出せるほか、関連する作業のときに Claude が自動で読み込む。

### いつ書くべきか
明確な基準がある：
> **同じ3段落の文脈を、週に2回以上貼り付けているなら、それはスキルにすべき。**

### 今保留にした理由
まだ実装を始めてもいないので、**繰り返しパターンが存在しない**。書きようがない。
実装が進み、「またこれを説明している」と気づいたら、その時が書き時。

### コツ（将来のために）
スキルには「gotchas（よくある落とし穴）」のセクションを必ず入れる。
正常系の手順書より、こちらの方が信頼性に効く。

---

## 4. `/loop` コマンド

### 何か
一定の時間間隔でプロンプトを繰り返し実行する。
`/goal`（条件が満たされるまで続ける）とは別物。

### 使いどころ（将来）
- デプロイが成功したか定期的に確認する
- CI パイプラインを監視する

### 今は不要
定期監視するようなものがまだ何もない。

---

## 5. 並列エージェント（git worktrees）

### 何か
複数の Claude セッションを、独立した作業コピーで同時に走らせる。

### 現実的な上限
**2〜3個が限界**とされる。理由は「自分のレビュー能力が律速になる」から。

### 今保留にした理由
- 並行処理は、私の認知特性（同時に2つ以上を保持できない）と正面から衝突する
- そもそもコードが読めないので、レビュー能力が律速になる前に破綻する
- **過去に並行ターミナルを使っていたのは、方針が固まっておらず1作業に時間がかかっていたため。**
  方針（憲法・設計書）が固まれば、そもそも必要なくなるはず。

---

## 6. MCP サーバー

### 何か
Claude Code に外部ツール（Notion、Google Calendar、データベースなど）へのアクセスを与える仕組み。

### 将来の関連性
このプロジェクトは Notion と Google Calendar を使っているので、
**いずれ関係してくる可能性は高い**。
ただし、今は実装そのものが始まっていないので、後回しでよい。

---

## 7. Plugins（プラグイン）

### 何か
Skills、Subagents、Hooks、MCP設定などを**ひとまとめにして配布**できるパッケージ。

### 今は不要
チームで共有する必要がない（一人で作っている）ため。

---

## 導入の順番（もし将来使うなら）

1. **permissions 設定** ← 今すぐ（憲法に採用済み）
2. **Skills** ← 繰り返しに気づいたとき
3. **Hooks** ← 「絶対にこれをやってほしくない」が明確になったとき
4. **Subagents** ← 思想が実装の中で安定してから
5. **MCP** ← Notion / Google Calendar 連携を実装するとき
6. 並列実行、Plugins ← おそらく不要

---

## 大原則（忘れないこと）

> **CLAUDE.md と Skills は「お願い」であり、「保証」ではない。**
> 絶対に起きなければならないことは、Hooks か permissions で強制する。

> **「多い方が良い」ではなく、「簡潔であることが性能要件である」。**
> CLAUDE.md が膨らむと、かえって Claude の精度が落ちる。200行未満を保つ。
